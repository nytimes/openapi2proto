syntax = "proto3";

package lowercaseapi;

import "google/protobuf/empty.proto";

message Item {}

service LowercaseAPIService {
    // Lowercase Response
    // 
    // This doesn't use camel-casing.
    rpc GetLowercaseDef(google.protobuf.Empty) returns (Item) {}
}